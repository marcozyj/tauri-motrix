export type RowSelectMethod = "all" | "none" | "invert" | "single" | "multiple";

export const enum FILE_SELECTION {
  Video = "video",
  Audio = "audio",
  Image = "image",
  Document = "document",
}
